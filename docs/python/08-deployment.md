# Ragent Python 版 — 部署架构与可观测性

## 1. 部署架构

### 1.1 单机部署（开发/小规模）

```
┌──────────────────────────────────────────────────────────────┐
│                     单机部署架构                               │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  Uvicorn + FastAPI                                       │ │
│  │  (uvloop 加速, 多 worker 进程)                           │ │
│  │                                                          │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │ │
│  │  │ Worker 1 │  │ Worker 2 │  │ Worker 3 │  │Worker N│ │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └────────┘ │ │
│  └──────────────────────────┬──────────────────────────────┘ │
│                              │                                │
│  ┌──────────┐  ┌────────────┼────────────┐  ┌────────────┐  │
│  │ PostgreSQL│  │   Redis    │            │  │  RabbitMQ  │  │
│  │ +pgvector │  │  缓存/限流  │            │  │  消息队列   │  │
│  └──────────┘  └────────────┘            │  └────────────┘  │
│                                           │                   │
│                              ┌────────────▼──────────────┐  │
│                              │  Milvus (可选)             │  │
│                              │  向量数据库                │  │
│                              └───────────────────────────┘  │
│                                                               │
│                              ┌───────────────────────────┐  │
│                              │  外部模型服务              │  │
│                              │  智谱 / OpenAI / Ollama   │  │
│                              └───────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 容器化部署（生产推荐）

```
┌──────────────────────────────────────────────────────────────────┐
│                    Docker Compose 编排                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  ragent-api                                                 │ │
│  │  FastAPI 应用容器                                            │ │
│  │  ├── uvicorn + uvloop                                       │ │
│  │  ├── 多 worker (根据 CPU 核数)                               │ │
│  │  └── 健康检查 /health                                        │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │  ragent-worker   │  │  postgres        │  │  redis         │ │
│  │  MQ 消费者容器   │  │  + pgvector      │  │  缓存/限流/锁  │ │
│  │  入库Pipeline    │  │  数据持久化      │  │  数据持久化    │ │
│  │  异步任务处理    │  │                  │  │                │ │
│  └──────────────────┘  └──────────────────┘  └────────────────┘ │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐                     │
│  │  rabbitmq        │  │  milvus          │                     │
│  │  消息队列        │  │  向量数据库       │                     │
│  │  管理界面 15672  │  │  (生产环境推荐)   │                     │
│  └──────────────────┘  └──────────────────┘                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  网络隔离:                                                │   │
│  │  ├── frontend — 对外暴露 API 端口                        │   │
│  │  └── backend  — 内部服务间通信                           │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 部署拓扑对比

| 组件          | 开发环境          | 生产环境                |
|--------------|-------------------|------------------------|
| 应用服务      | 单 worker         | 多 worker + 多副本      |
| PostgreSQL   | 单实例 + pgvector | 主从 + pgvector         |
| Redis        | 单实例            | Sentinel 哨兵模式       |
| RabbitMQ     | 单节点            | 镜像队列集群            |
| Milvus       | 可选（用pgvector）| Standalone / Cluster   |
| 向量存储      | pgvector          | Milvus（推荐）          |

---

## 3. 可观测性

### 3.1 全链路追踪

基于装饰器驱动的 Trace 框架，覆盖 RAG 问答的每一个环节：

```
@rag_trace_root("rag-chat")
  ├── @rag_trace_node("query-rewrite")     — 问题重写耗时
  ├── @rag_trace_node("intent-classify")   — 意图分类耗时
  ├── @rag_trace_node("retrieval")         — 多路检索耗时
  │     ├── @rag_trace_node("channel-1")   — 单通道耗时
  │     └── @rag_trace_node("channel-2")   — 单通道耗时
  ├── @rag_trace_node("rerank")            — 重排序耗时
  ├── @rag_trace_node("prompt-build")      — Prompt 组装耗时
  └── @rag_trace_node("llm-generate")      — LLM 生成耗时
```

每次 Trace 产生两类记录：

- **t_rag_trace_run** — 记录整体运行信息：trace_id、状态、总耗时、错误信息
- **t_rag_trace_node** — 记录每个节点的详细信息：支持树形嵌套、深度、模块名、函数名、额外数据

### 3.2 入库过程追踪

```
┌───────────────────────────────────────────────────────────┐
│  入库追踪体系                                              │
│                                                            │
│  任务级 (t_ingestion_task):                                │
│  ├── 整体状态 (PENDING/RUNNING/COMPLETED/FAILED)          │
│  ├── 总耗时                                                │
│  └── 错误信息                                              │
│                                                            │
│  节点级 (t_ingestion_task_node):                           │
│  ├── 每个节点的独立执行记录                                │
│  ├── 节点状态 / 耗时 / 输出                                │
│  └── 错误详情                                              │
│                                                            │
│  分块级 (t_knowledge_document_chunk_log):                  │
│  ├── 提取耗时 / 分块耗时 / 向量化耗时 / 持久化耗时        │
│  └── 分块数量 / 错误信息                                   │
└───────────────────────────────────────────────────────────┘
```

### 3.3 结构化日志

```
┌──────────────────────────────────────────────────────────┐
│  日志规范                                                 │
│                                                           │
│  输出格式: JSON                                           │
│  ├── timestamp  — 时间戳                                  │
│  ├── level      — 日志级别 (DEBUG/INFO/WARNING/ERROR)    │
│  ├── trace_id   — 链路追踪ID (自动绑定)                   │
│  ├── module     — 模块名                                  │
│  ├── message    — 日志消息                                 │
│  └── extra      — 扩展字段 (模型名/耗时/状态码等)         │
│                                                           │
│  日志级别使用规范:                                        │
│  ├── DEBUG   — 开发调试信息                               │
│  ├── INFO    — 关键业务节点 (请求进入/检索完成/入库完成)   │
│  ├── WARNING — 可恢复的异常 (模型切换/检索超时)           │
│  └── ERROR   — 不可恢复错误 (数据库连接失败/全链路失败)    │
└──────────────────────────────────────────────────────────┘
```

---

## 4. 健康检查

```
┌──────────────────────────────────────────────────────────┐
│  /health 端点                                             │
│                                                           │
│  检查项:                                                  │
│  ├── PostgreSQL 连接     → SELECT 1                       │
│  ├── Redis 连接          → PING                           │
│  ├── RabbitMQ 连接       → 连接状态检查                   │
│  ├── Milvus 连接 (可选)  → Collection 列表                │
│  └── 磁盘空间            → 临时目录可用空间                │
│                                                           │
│  响应:                                                    │
│  ├── 200  — 全部正常                                      │
│  └── 503  — 部分组件不可用（附带详情）                     │
└──────────────────────────────────────────────────────────┘
```

---

## 5. 配置管理

```
┌──────────────────────────────────────────────────────────┐
│  配置优先级 (从高到低)                                     │
│                                                           │
│  1. 环境变量        → RAGENT_REDIS_URL=...               │
│  2. .env 文件       → 本地开发用                          │
│  3. config.yaml     → 默认配置                            │
│  4. 代码内默认值    → Pydantic Settings 定义              │
│                                                           │
│  关键配置项:                                              │
│  ├── RAGENT_DATABASE_URL    — PostgreSQL 连接串           │
│  ├── RAGENT_REDIS_URL       — Redis 连接串                │
│  ├── RAGENT_MQ_URL          — RabbitMQ 连接串             │
│  ├── RAGENT_MILVUS_HOST     — Milvus 地址 (可选)         │
│  ├── RAGENT_LITELLM_CONFIG  — 模型配置文件路径            │
│  ├── RAGENT_MAX_CONCURRENT  — 最大并发数                  │
│  └── RAGENT_LOG_LEVEL       — 日志级别                    │
└──────────────────────────────────────────────────────────┘
```

---

## 6. 与 Java 版的简化对比

| 维度             | Java 版                      | Python 版                     |
|-----------------|------------------------------|-------------------------------|
| 前端             | React 18 完整前端            | 无前端，纯 API 服务           |
| 鉴权             | Sa-Token                    | 基础 Token 或无鉴权           |
| MCP Server      | 独立 Spring Boot 进程        | 内聚到主应用                  |
| 消息队列         | RocketMQ 5.x                | RabbitMQ                      |
| 入库路线         | 同步 + MQ 两条路线           | 仅 MQ 异步路线                |
| 线程模型         | 8 个独立线程池               | asyncio 协程                  |
| 文档解析         | Apache Tika                 | pdfplumber + python-docx 等   |
| 模型调用         | 自定义 ChatClient 体系       | litellm 统一封装              |
| 熔断器           | 自定义 ModelHealthStore      | circuitbreaker 库             |
| 容器化           | 未明确                       | Docker Compose 一键部署       |
